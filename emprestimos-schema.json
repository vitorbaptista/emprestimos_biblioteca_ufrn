{
  "fields": [
    {
      "name": "id_emprestimo",
      "type": "integer",
      "format": "default",
      "description": "Identificador do empréstimo",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "codigo_barras",
      "type": "string",
      "format": "default",
      "description": "Código de barras do item do acervo",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "data_renovacao",
      "type": "date",
      "format": "%Y/%m/%d %H:%M:%S.%f000",
      "description": "Data da renovação do empréstimo",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "data_emprestimo",
      "type": "date",
      "format": "%Y/%m/%d %H:%M:%S.%f000",
      "description": "Data da realização do empréstimo",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "data_devolucao",
      "type": "date",
      "format": "%Y/%m/%d %H:%M:%S.%f000",
      "description": "Data da devolução do item",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "matricula_ou_siape",
      "type": "integer",
      "format": "default",
      "description": "Matrícula ou SIAPE do usuário que realizou o empréstimo",
      "constraints": {
        "required": false
      }
    },
    {
      "name": "nome_usuario",
      "type": "string",
      "format": "default",
      "description": "Nome do usuário que realizou o empréstimo",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "tipo_vinculo_usuario",
      "type": "string",
      "format": "default",
      "description": "Tipo do vínculo do usuário (servidor, discente, docente, etc.)",
      "constraints": {
        "enum": [
          "ALUNO DE GRADUAÇÃO",
          "ALUNO DE PÓS-GRADUAÇÃO",
          "ALUNO MÉDIO/TÉCNICO",
          "DOCENTE",
          "DOCENTE EXTERNO",
          "SERVIDOR TÉCNICO-ADMINISTRATIVO",
          "USUÁRIO EXTERNO"
        ],
        "required": true
      }
    }
  ]
}
